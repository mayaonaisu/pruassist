# === Secrets — NEVER commit ===
.env
.env.local
**/.env.local
**/.env*.local

# === Dependencies & build output ===
node_modules/
**/node_modules/
.next/
**/.next/
out/
build/
*.tsbuildinfo
# onnxruntime-web's WASM runtime, copied from node_modules by scripts/copy-ort-wasm.mjs on postinstall.
# Regenerated, so never committed. (The NeXt-TDNN model under public/models/*.onnx IS committed.)
public/ort/

# === Not part of the app: input PDFs, private recordings, local binaries, generated docs ===
*.pdf
# ...except the committed brochure PDFs the sharing-mode whiteboard renders.
!public/docs/*.pdf
*.mp4
*.wav
*.docx
cloudflared.exe
transcribe.py
prudential_12june_transcript.txt

# === Tooling / editor / OS ===
.claude/
.vscode/
.DS_Store
Thumbs.db
*.log

# === Local launcher (machine-specific paths) ===
# Start-PRUAssist.bat is committed as a reference but tuned for the original PC.

# === AI-agent instructions (kept locally, not published) ===
**/AGENTS.md
**/CLAUDE.md

# === Next.js / Vercel specifics ===
.vercel
next-env.d.ts
/coverage
*.pem
.pnp
.pnp.*

.vercel
.env*
